<%= @post.name %>さま

以下の内容で返信がありました。

投稿内容:
<%= @post.title %>
<%= @post.text %>

投稿日時:
<%= @post.created_at.strftime("%Y?%m/%d %H:%M") %>

返信内容:
<%= @reply.text %>

<%= @reply.name %>

返信日時:
<%= @reply.created_at.strftime("%Y?%m/%d %H:%M") %>

この返信に対する回答は直接以下のメールアドレスにお願いいたします。
<%= @reply.email %>

ご利用ありがとうございました。

沖縄宝島
okinawatakarajima.com